spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/hanpenggit/springCloudDemo
          search-paths: configWarehouse
          default-label: master

server:
  port: 8888
